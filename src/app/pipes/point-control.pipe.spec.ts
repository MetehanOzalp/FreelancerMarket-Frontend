import { PointControlPipe } from './point-control.pipe';

describe('PointControlPipe', () => {
  it('create an instance', () => {
    const pipe = new PointControlPipe();
    expect(pipe).toBeTruthy();
  });
});
